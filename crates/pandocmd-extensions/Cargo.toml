[package]
name = "pandocmd-extensions"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Pandoc Markdown extension model: all extensions, flavor defaults, and format-spec parsing"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
